[".config/nvim"]
    type = "git-repo"
    # This repository is public. Keep bootstrap dependencies on HTTPS: chezmoi
    # installs externals before it runs scripts that configure the SSH agent.
    url = "https://github.com:443/karolis-arbaciauskas/kickstart.nvim.git"
    refreshPeriod = "1h"
