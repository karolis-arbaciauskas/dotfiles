export GOPATH="{{ .env.paths.go }}"

# 1Password SSH agent (used for auth and Git SSH signing)
export SSH_AUTH_SOCK="{{ .chezmoi.homeDir }}/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"

export EDITOR="${EDITOR:-{{ .env.editor }}}"
command -v "$EDITOR" >/dev/null 2>&1 || export EDITOR="vi"

export LC_ALL="{{ .env.locale.lc_all }}"
export LANG="{{ .env.locale.lang }}"
